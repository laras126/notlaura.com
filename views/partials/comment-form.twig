<div class="comment-form">
	<h4>Submit a Comment</h4>
	<form id="form" class="comment-form" method="post" action="{{ site.url~'/wp-comments-post.php' }}">
		{% if user %}
			<input type="hidden" name="email" value="{{ user.email }}">
			<input type="hidden" name="author" value="{{ user.name }}">
			<input type="hidden" name="url" value="{{ user.link }}">
		{% else %}
			<div class="form-user-info">
				<div class="form-author">
					<label for="author" class="frm_primary_label">Name <span class="frm_required">*</span></label>
					<input placeholder="Juan Snow" required name="author" type="text" id="author"/>
				</div>
				<div class="form-email">
					<label for="email" class="frm_primary_label">Email <span class="frm_required">*</span></label>
					<input placeholder="dragons@superb.com" required name="email" type="text" id="email"/>
				</div>
			</div>
		{% endif %}

		<div class="form-comment">
			<label for="comment" class="frm_primary_label">Comment <span class="frm_required">*</span></label>
			<textarea placeholder="Comment text" name="comment" cols="60" rows="3" id="comment"></textarea>
		</div>

		<input name="comment_post_ID" value="{{ post.id }}" id="comment_post_ID" type="hidden">
		<input name="comment_parent" id="comment_parent" value="{{ comment.ID|default('0') }}" type="hidden">

		<div>
			<button type="submit" name="Submit" class="btn">Submit Comment</button>
			<button class="btn-reset" type="reset">Reset form</button>
		</div>
		<p class="comments-note">Pssst...developer note: This is the most basic comment implentation you can possibly imagine, and it's on my list to flesh it out. But you know what? I'm not using Disqus anymore, so that's cool! 👍</p>
	</form>
</div>
